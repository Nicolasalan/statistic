#! /usr/bin/env python3

# Autor: Nicolas Alan

import scipy.stats as stats
import math

def hypothese(average: float, x: float, n: int, standard: float, case: int) -> float:
     """
     Summary:
          Test Z: (X̄ - μ) / (σ / √n)
     Params:
          X̄: average sample
          μ: population average
          σ: population standard deviation
          n: sample size
     """

     if case == 0:
          # TODO: ...
          pass

if __name__ == '__main__':

     norm(average, x, n, standard, case)